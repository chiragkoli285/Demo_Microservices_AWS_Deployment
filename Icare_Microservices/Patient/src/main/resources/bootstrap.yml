server:
  port: 8100


spring:
  application:
    name: PatientMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        defaultContext: application
        data-key: data
        profileSeparator: '::'
        format: YAML